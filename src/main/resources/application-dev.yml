# ── Development profile ───────────────────────────────────────────────────────
# Used when SPRING_PROFILES_ACTIVE=dev (set in docker-compose.yml)
# NEVER use these values in production.

txguard:
  security:
    api-key: txguard-dev-key-12345
